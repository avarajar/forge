{
  "name": "forge",
  "private": true,
  "scripts": {
    "start": "pnpm install && pnpm build && node packages/platform/dist/index.js",
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.9.3",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=11.0.0"
  }
}
